export const actions = {
	login() {
		// commit('SET_TOKEN', payload.token)
	},
}

export const mutations = {
	setState() {
		// commit('SET_TOKEN', payload.token)
	},
}

export default {
	namespaced: true,
	state: {
		currentUser: null,
	},
}
